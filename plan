# vim: ft=markdown

# TODO

cron in chef configuration
- rudimentary tasks? (once per week - avoid schedule issues)

Widget in /profile tab

Run restore on production

# DONE

Design
`users.points_history` - array with $slice (need to upgrade to 2.4)

rph: [
    { perl: 0, chaos: 5 },
    { perl: 0, chaos: 6 },
    { perl: 3, chaos: 6 },
    { perl: 50, chaos: 7 }
]

Store statistics (per week, 9 times)
Retroactively restore statistics (from backups)

Tooltip

mobile
vertical-align
check for follow button + long login

## Restore details

for each tar.gz:
    - unpack
    - restore
    - save users.rp somewhere

