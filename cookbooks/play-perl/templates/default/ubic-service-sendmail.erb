# vim: ft=perl
use parent qw(Ubic::Service::SimpleDaemon);

__PACKAGE__->new({
    bin => '/play/backend/pumper/sendmail.pl --loop --sleep-period=5',
    stdout => '/data/pumper/sendmail.log',
    stderr => '/data/pumper/sendmail.err.log',
    ubic_log => '/data/pumper/sendmail.ubic.log',
    reload_signal => 'HUP',
});
