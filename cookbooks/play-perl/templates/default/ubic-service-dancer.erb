# vim: ft=perl
use strict;
use warnings;

use Ubic::Service::Plack;

Ubic::Service::Plack->new({
    server => 'Starman',
    server_args => {
        port => 3000,
    },
    env => {
        PLACK_ENV => 'production',
    },
    app => "bin/app.pl",
    cwd => "/play/app",
    stdout => '/data/dancer/stdout.log',
    stderr => '/data/dancer/stderr.log',
    ubic_log => '/data/dancer/ubic.log',
});
