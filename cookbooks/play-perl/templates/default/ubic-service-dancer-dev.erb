# vim: ft=perl
use strict;
use warnings;

use Ubic::Service::SimpleDaemon;

Ubic::Service::SimpleDaemon->new({
    env => {
        DEV_MODE => 1, # enables some development-only routes
    },
    bin => "bin/app.pl --port 3001 --environment development",
    cwd => "/play/app",
    stdout => '/data/dancer-dev/stdout.log',
    stderr => '/data/dancer-dev/stderr.log',
    ubic_log => '/data/dancer-dev/ubic.log',
});
