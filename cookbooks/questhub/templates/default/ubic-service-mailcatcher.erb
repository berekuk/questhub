# vim: ft=perl
use parent qw(Ubic::Service::SimpleDaemon);

__PACKAGE__->new({
    bin => 'mailcatcher --ip=0.0.0.0 -f',
    cwd => '/tmp',
    stdout => '/data/mailcatcher.log',
    stderr => '/data/mailcatcher.err.log',
    ubic_log => '/data/mailcatcher.ubic.log',
});
